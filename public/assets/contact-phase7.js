(function(){
  const form=document.getElementById('contact-form'); if(!form) return;
  const en=/(^|\/)en(\/|$)/.test(decodeURIComponent(location.pathname));
  const btn=form.querySelector('button[type="submit"]'); const status=document.getElementById('contact-status');
  const original=btn?.textContent || (en?'Send inquiry':'お問い合わせを送信する');
  const tx=en?{sending:'Sending your inquiry…',ok:'Your inquiry has been received. Opening the confirmation page…',err:'We could not send the message. Please check your connection and try again. If the problem continues, contact us via Instagram @retro_hiyori.'}:{sending:'お問い合わせを送信しています。',ok:'送信を受け付けました。完了ページへ移動します。',err:'送信できませんでした。通信状況をご確認のうえ、もう一度お試しください。改善しない場合はInstagram @retro_hiyori からご連絡ください。'};
  function setState(message,type){if(!status)return;status.textContent=message||'';status.dataset.state=type||'';}
  form.addEventListener('submit',async function(e){e.preventDefault();if(!form.reportValidity())return;if(btn){btn.disabled=true;btn.textContent=en?'Sending…':'送信中…';}setState(tx.sending,'sending');const payload={};new FormData(form).forEach((value,key)=>{payload[key]=value;});payload['_url']=location.href;payload[en?'Submitted at':'送信日時']=new Date().toLocaleString(en?'en-US':'ja-JP',{timeZone:'Asia/Tokyo'});try{const res=await fetch('https://formsubmit.co/ajax/1831007109@qq.com',{method:'POST',headers:{'Content-Type':'application/json','Accept':'application/json'},body:JSON.stringify(payload)});let data={};try{data=await res.json();}catch(_e){}if(!res.ok||data.success===false)throw new Error(data.message||'Submission failed');setState(tx.ok,'success');window.location.href='contact-thanks.html';}catch(err){console.error('Retro日和 contact form:',err);setState(tx.err,'error');if(btn){btn.disabled=false;btn.textContent=original;}}});
})();